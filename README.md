# habba-store

## Habba AI mode (Vercel env)

For free local AI behavior by default, set:

- `HABBA_AI_MODE=local`

`OPENAI_API_KEY` is optional in local mode. It can remain configured, but it will not be used unless:

- `HABBA_AI_MODE=openai`
